apiVersion: v1
kind: Service
metadata:
  name: grafana-service
  namespace: hello-world-ops
  labels:
    app: grafana
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
  selector:
    app: grafana 