[project]
name = "learn-agentic-py"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "python-dotenv",
    "langchain",
    "langchain-community",
    "langchain-groq",
]
